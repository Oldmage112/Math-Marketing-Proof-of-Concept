# Embeddings

This folder can store vector index configuration, sample embedding records, or notes about the vector database used for the proof of concept.

Avoid committing large production embedding indexes unless they are small, sanitized, and intended for sharing.
